<div class="max-w-screen-xl mx-auto py-12 px-4 overflow-hidden sm:px-6 lg:px-8">
  <nav class="-mx-5 -my-2 flex flex-wrap justify-center">
    <div class="px-5 py-2">
      <% if current_user.present? %>
        <a href="<%= sign_out_path %>" class="text-base leading-6 text-gray-500 hover:text-primary-600">
          Sign Out
        </a>
      <% else %>
        <a href="sign_in" class="text-base leading-6 text-gray-500 hover:text-primary-600">
          Sign In
        </a>
      <% end %>
    </div>
  </nav>

  <div class="mt-8">
    <p class="text-center text-base leading-6 text-gray-400">
      &copy; 2020 your_app_name
    </p>
  </div>
</div>